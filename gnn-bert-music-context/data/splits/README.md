# Dataset splits

The JSON files contain starter records for smoke testing. Replace these records with official, leakage-safe train/validation/test splits after downloading and preprocessing the selected datasets.
