# Processed data

Generated artifacts belong in these folders:

- `graphs/`: serialized PyG graphs or JSON graph exports
- `audio_features/`: extracted mel, chroma, and MFCC features
- `bert_cache/`: cached tokenization or text embeddings

The checked-in files are small smoke-test fixtures only. Replace them with outputs from the real datasets before training.
